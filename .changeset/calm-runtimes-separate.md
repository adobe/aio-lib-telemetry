---
"@adobe/aio-lib-telemetry": minor
---

Expose stable resource attributes through `getAioRuntimeResourceAttributes` and current invocation attributes through `getAioRuntimeInvocationAttributes`. Deprecate `getAioRuntimeAttributes` while preserving its combined output for compatibility.

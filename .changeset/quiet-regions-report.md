---
"@adobe/aio-lib-telemetry": minor
---

Expose the region selected at execution time as `action.region` on spans and exported logs. The value may differ between invocations because Runtime routes each invocation independently.
